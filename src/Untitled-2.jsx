import * as React from 'react'

export default (props) => {
  return (
    <>
    {(asdf) ?  this  :  that }

<div class={`content__main${(article.hasRelatedContents) && "content__main--has-related-articles$"}${(isShortVariation) && "content__main--short$"}`}
/* HBS2JSX IGNORED {{#if giftStatus.isEnabled}} data-js-gift-enabled=true{{/if}} */  data-js-gift-enabled={(giftStatus.isEnabled) && "true"}
"/* HBS2JSX IGNORED {{title}} */">
  {items.map(item =>  subTitle : {subTitle} )}
</div>

{items.map(item =>  <div><div></div><p><img/></p></div> )}
{items.map(item =>   <div asdf=/* HBS2JSX IGNORED {{asdf}} */>
   asdf
  </div>
)}
<Asdf {...items} {...title} title="1">
{child.parentToo}
{child.onlyParent}

<img src="dasdfsdfasdf"/>

adsfasdfas
asdfasd

{title}

    </>
  )
}